/// <reference path="../../libs/TSDef/p5.global-mode.d.ts" />

function setup() {
  createCanvas(500, 500);
}

function draw() {
  background("black");
  fill("white");
  text("the name of the \"song\" is all that\nand I like it because it's a jazzy bop", 100, 100, 400, 400);
  
}
